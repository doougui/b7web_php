<?php 
	// - Files MUST use only <?php and <?= tags (não se deve usar tag de fechamento para o <?php).
	// - Files MUST use only UTF-8 without BOM for PHP code (http://prntscr.com/owtd75).
	// - Files SHOULD either declare symbols (classes, functions, constants, etc.) or cause side-effects (e.g. generate output, change .ini settings, etc.) but SHOULD NOT do both (usar sistema de template engine para views).
	// - Namespaces and classes MUST follow an “autoloading” PSR: [PSR-0, PSR-4].
	// - Class names MUST be declared in StudlyCaps (no caso de controllers, por exemplo, ficaria HomeController, e não homeController).
	// - Class constants MUST be declared in all upper case with underscore separators (Ex: DATA_DE_CRIACAO, VERSION).
	// - Method names MUST be declared in camelCase (Ex: getNome(), primeira letra minúscula).